# BASE TRADING BOT - CONFIGURATION COMPLÈTE
# ⚠️ NE JAMAIS COMMITER CE FICHIER DANS GIT
# Copier en .env et remplir vos valeurs

# ============================================
# 🌐 BLOCKCHAIN CONFIGURATION
# ============================================
RPC_URL=https://base.drpc.org
RPC_BACKUP_1=https://mainnet.base.org
RPC_BACKUP_2=https://base.publicnode.com
RPC_BACKUP_3=https://base.meowrpc.com
BASE_CHAIN_ID=8453

# Configuration DRPC (optionnel)
DRPC_API_KEY=
DRPC_RATE_LIMIT=100

# Adresses des contrats
WETH_ADDRESS=0x4200000000000000000000000000000000000006
USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# ============================================
# 🔑 WALLET (CONFIDENTIEL - REQUIS)
# ============================================
WALLET_ADDRESS=YOUR_WALLET_ADDRESS
PRIVATE_KEY=YOUR_PRIVATE_KEY_WITHOUT_0x

# ============================================
# 📊 APIS EXTERNES (CONFIDENTIEL)
# ============================================
# Une seule clé Etherscan pour toutes les chaînes EVM
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY
COINGECKO_API_KEY=YOUR_COINGECKO_API_KEY

# ============================================
# 💾 DATABASE
# ============================================
DATABASE_PATH=data/trading.db
BACKUP_PATH=data/backups/

# ============================================
# ⚙️ TRADING STRATEGY - CONFIGURABLE
# ============================================
# Mode: paper (simulation) ou real (production)
TRADING_MODE=paper

# Position sizing
POSITION_SIZE_PERCENT=15     # Taille de position en % du capital (défaut: 15%)
MAX_POSITIONS=2              # Nombre max de positions simultanées (défaut: 2)
MAX_TRADES_PER_DAY=3         # Nombre max de trades par jour (défaut: 3)

# Risk management - STOP LOSS
STOP_LOSS_PERCENT=5          # Stop loss normal en % (défaut: 5%)

# 🔧 GRACE PERIOD - NOUVEAU! Configurable depuis .env
# Période de tolérance au début d'une position pour éviter sorties prématurées
GRACE_PERIOD_ENABLED=true           # Activer/désactiver grace period (défaut: true)
GRACE_PERIOD_MINUTES=3              # Durée de la grace period en minutes (défaut: 3)
GRACE_PERIOD_STOP_LOSS=35           # Stop loss pendant grace period en % (défaut: 35%)
# Exemple: Pendant 3 premières minutes, SL à -35% au lieu de -5% pour éviter sorties sur slippage initial

# Trailing stop
TRAILING_ACTIVATION_THRESHOLD=12    # Activation du trailing stop à +12% (défaut: 12%)

# Monitoring
MONITORING_INTERVAL=1               # Intervalle de monitoring en secondes (défaut: 1)

# Token approval expiration
TOKEN_APPROVAL_MAX_AGE_HOURS=12     # Expiration des tokens approuvés en heures (défaut: 12)

# Cooldown pour tokens rejetés
REJECTED_TOKEN_COOLDOWN_MINUTES=30  # Cooldown pour tokens rejetés en minutes (défaut: 30)

# ============================================
# 🔍 SCANNER CONFIGURATION - CONFIGURABLE
# ============================================
SCAN_INTERVAL_SECONDS=30     # Intervalle entre scans en secondes (défaut: 30)
MAX_BLOCKS_PER_SCAN=100      # Nombre max de blocs par scan (défaut: 100)
SCANNER_START_BLOCK=0        # Bloc de départ du scanner (défaut: 0)

# 🔧 FILTRAGE D'ÂGE PAR LE SCANNER - NOUVEAU!
# Le Scanner filtre les tokens par âge avant de les stocker en DB
# Seuls les tokens dans la tranche MIN_TOKEN_AGE_HOURS à MAX_TOKEN_AGE_HOURS sont découverts
MIN_TOKEN_AGE_HOURS=2        # Âge minimum d'un token pour être découvert (défaut: 2h)
MAX_TOKEN_AGE_HOURS=12       # Âge maximum d'un token pour être découvert (défaut: 12h)
# Exemple: Scanner ne découvrira QUE les tokens entre 2h et 12h d'âge
# Tokens <2h: Ignorés (trop jeunes, trop volatils)
# Tokens >12h: Ignorés (trop vieux, opportunité manquée)

# Factories à surveiller
UNISWAP_V3_FACTORY=0x33128a8fC17869897dcE68Ed026d694621f6FDfD
AERODROME_FACTORY=0x420DD381b31aEf6683db6B902084cB0FFECe40Da

# ============================================
# 🎯 FILTER CONFIGURATION - CONFIGURABLE
# ============================================
FILTER_INTERVAL_SECONDS=60   # Intervalle entre filtrage en secondes (défaut: 60)

# Critères de sélection des tokens
MIN_AGE_HOURS=2              # Âge minimum du token en heures (défaut: 2)
MIN_LIQUIDITY_USD=30000      # Liquidité minimum en USD (défaut: 30000)
MAX_LIQUIDITY_USD=10000000   # Liquidité maximum en USD (défaut: 10000000)
MIN_VOLUME_24H=50000         # Volume 24h minimum en USD (défaut: 50000)
MIN_HOLDERS=150              # Nombre minimum de holders (défaut: 150)
MIN_MARKET_CAP=25000         # Market cap minimum en USD (défaut: 25000)
MAX_MARKET_CAP=10000000      # Market cap maximum en USD (défaut: 10000000)
MAX_OWNER_PERCENTAGE=10.0    # % maximum détenu par le owner (défaut: 10%)
MAX_BUY_TAX=5                # Taxe d'achat maximum en % (défaut: 5%)
MAX_SELL_TAX=5               # Taxe de vente maximum en % (défaut: 5%)

# Scores minimums
MIN_SAFETY_SCORE=70          # Score de sécurité minimum (défaut: 70)
MIN_POTENTIAL_SCORE=60       # Score de potentiel minimum (défaut: 60)

# ============================================
# 📈 TRAILING STOP CONFIGURATION - CONFIGURABLE
# ============================================
# Niveau 1: 12-30% profit → trailing stop à -3%
TRAILING_L1_MIN=12
TRAILING_L1_MAX=30
TRAILING_L1_DISTANCE=3

# Niveau 2: 30-100% profit → trailing stop à -5%
TRAILING_L2_MIN=30
TRAILING_L2_MAX=100
TRAILING_L2_DISTANCE=5

# Niveau 3: 100-300% profit → trailing stop à -10%
TRAILING_L3_MIN=100
TRAILING_L3_MAX=300
TRAILING_L3_DISTANCE=10

# Niveau 4: 300%+ profit → trailing stop à -30%
TRAILING_L4_MIN=300
TRAILING_L4_MAX=99999
TRAILING_L4_DISTANCE=30

# ============================================
# ⏱️ TIME EXIT CONFIGURATION - CONFIGURABLE
# ============================================
# Stagnation: sortie si profit < 5% après 24h
TIME_EXIT_STAGNATION_HOURS=24
TIME_EXIT_STAGNATION_MIN_PROFIT=5

# Low momentum: sortie si profit < 20% après 48h
TIME_EXIT_LOW_MOMENTUM_HOURS=48
TIME_EXIT_LOW_MOMENTUM_MIN_PROFIT=20

# Maximum: sortie forcée après 72h
TIME_EXIT_MAXIMUM_HOURS=72

# Emergency: sortie forcée après 120h
TIME_EXIT_EMERGENCY_HOURS=120

# ============================================
# 🔧 ADVANCED SETTINGS - CONFIGURABLE
# ============================================
# Gas settings
MAX_GAS_PRICE_GWEI=50        # Prix gas maximum en Gwei (défaut: 50)
GAS_LIMIT_BUY=250000         # Gas limit pour achat (défaut: 250000)
GAS_LIMIT_SELL=300000        # Gas limit pour vente (défaut: 300000)

# Slippage
MAX_SLIPPAGE_PERCENT=3       # Slippage maximum en % (défaut: 3%)
EMERGENCY_SLIPPAGE_PERCENT=5 # Slippage d'urgence en % (défaut: 5%)

# Retry settings
MAX_RETRIES=3                # Nombre max de tentatives (défaut: 3)
RETRY_DELAY_SECONDS=2        # Délai entre tentatives en secondes (défaut: 2)

# ============================================
# 📊 DASHBOARD (OPTIONNEL) - CONFIGURABLE
# ============================================
DASHBOARD_PORT=8501
DASHBOARD_URL=http://localhost:8501
DASHBOARD_USER=admin
DASHBOARD_PASSWORD=changeme_please

# ============================================
# 🚨 ALERTING (OPTIONNEL)
# ============================================
ALERTS_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ============================================
# 📝 LOGGING - CONFIGURABLE
# ============================================
LOG_LEVEL=INFO               # Niveau de log: DEBUG, INFO, WARNING, ERROR (défaut: INFO)
LOG_FILE=logs/trading.log
LOG_MAX_SIZE_MB=100
LOG_BACKUP_COUNT=5
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ============================================
# 🔐 SECURITY
# ============================================
# Activer 2FA pour les trades réels
REQUIRE_2FA=false
TWO_FA_SECRET=

# IP Whitelist (comma separated)
ALLOWED_IPS=127.0.0.1,localhost

# ============================================
# 🧪 DEBUG (Dev only)
# ============================================
DEBUG_MODE=false
DRY_RUN=false
SIMULATE_BLOCKCHAIN=false
